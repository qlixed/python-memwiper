language: python

cache: pip

matrix:
    include:
        - python: "3.3"
        - python: "3.6"
        - python: "nightly"

install: pip install tox

script: tox

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - gcc
